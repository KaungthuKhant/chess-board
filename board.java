public class board {
    
    // the board is a 2 d array with each elements begin a spot
    // a spot contains: floorColor, posAphabet, posNumber
    // check to make sure the next move is not a white piece
    //public String checkColorOfSpot(x, y)
}
